// Auto-generated from res/y.blueprint.js

export class BPNumber {
    public static toFixed(num, fractionDigits) {
        return num.toFixed(fractionDigits);
    }

    public static toExponential(num, fractionDigits) {
        return num.toExponential(fractionDigits);
    }

    public static toPrecision(num, precision) {
        return num.toPrecision(precision);
    }

    public static toString(num, radix) {
        return num.toString(radix);
    }
}
