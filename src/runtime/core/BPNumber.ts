// Auto-generated from res/y.blueprint.js

export class BPNumber {
    public static toFixed(num: number, fractionDigits?: number): string {
        return num.toFixed(fractionDigits);
    }

    public static toPrecision(num: number, fractionDigits?: number): string {
        return num.toPrecision(fractionDigits);
    }

    public static toExponential(num: number, precision?: number): string {
        return num.toExponential(precision);
    }

    public static toString(num: number, radix?: number): string {
        return num.toString(radix);
    }
}
